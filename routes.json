{
    "": [
        "middleware.contextualizer", "express.static", "express.logger", "middleware.logwriter",
        "express.query", "express.bodyParser", "express.cookieParser",
        "express.session", "app.router", "express.errorHandler"
    ],
    "/": {
        "get": ["middleware.dispatcher"]
    },
    "/register": {
        "post": ["middleware.auth.register"],
        "get": ["middleware.dispatcher"]
    },
    "/login": {
        "post": ["middleware.auth.enticate"],
        "get": ["middleware.dispatcher"]
    },
    "/logout": {
        "all": ["middleware.auth.logout"]
    }
}
