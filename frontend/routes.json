{
    "": [
        "middleware.registry", "middleware.contextualizer", "express.static", "express.logger",
        "middleware.logwriter", "express.query", "express.bodyParser", "express.cookieParser",
        "express.session", "express.csrf", "app.router", "express.errorHandler"
    ],
    "/": {
        "get": ["middleware.dispatcher"]
    },
    "/register": {
        "post": ["middleware.validator", "middleware.auth.register"],
        "get": ["middleware.dispatcher"]
    },
    "/login": {
        "post": ["middleware.validator", "middleware.auth.login"]
    },
    "/logout": {
        "all": ["middleware.auth.logout"]
    },
    "/test": {
        "get": ["middleware.dispatcher"]
    }
}
